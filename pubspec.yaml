name: flutter_application_6
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8
  file_picker: ^8.1.1  # อัปเดตเล็กน้อย
  syncfusion_flutter_pdf: ^21.1.35
  google_generative_ai: 0.4.4
  flutter_markdown: ^0.7.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 # แก้เลขเวอร์ชันให้เหมาะสม

flutter:
  uses-material-design: true